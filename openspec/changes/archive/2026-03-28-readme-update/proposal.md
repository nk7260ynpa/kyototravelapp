## Why

專案已從 Xcode 預設模板發展為具備四大模組（交通、景點、慶典、餐廳）與京都和風主題的完整骨架，但缺少 README.md，且 CLAUDE.md 仍描述為「初始階段」。需要更新文件以反映目前的程式碼結構。

## What Changes

- 新增 `README.md`，包含專案簡介、架構說明、建置指令
- 更新 `CLAUDE.md`，反映目前的檔案結構與架構
- 更新 `.gitignore`，加入常見 Xcode/iOS 忽略項目

## Capabilities

### New Capabilities

（無新增 capability — 純文件更新）

### Modified Capabilities

（無修改 capability）

## Impact

- 新增 `README.md`
- 修改 `CLAUDE.md`
- 修改 `.gitignore`
- 不影響任何程式碼
